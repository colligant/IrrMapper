import setuptools

setuptools.setup(
        name='trainer',
        version='0.1',
        packages=setuptools.find_packages(),
        include_package_data=True,
        description='Training application'
)
